name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r model/requirements.txt
          pip install -r dashboard/backend/requirements.txt

      - name: Compile model sources
        run: python -m compileall model/src

      - name: Check predictions markdown is current
        run: |
          python model/scripts/write_predictions_md.py
          git diff --exit-code docs/predictions.md docs/predictions-elo.svg

      - name: Check evaluation markdown is current
        run: |
          python model/scripts/write_evaluation_md.py
          git diff --exit-code docs/evaluation-results.md docs/evaluation-headline.svg docs/evaluation-elo-teams.svg

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: dashboard/frontend/package-lock.json

      - name: Build frontend
        working-directory: dashboard/frontend
        run: |
          npm ci
          npm run build
